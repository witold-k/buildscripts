pane_id=$(tmux list-panes -a -F '#{pane_id} #{pane_current_command}' | grep hx | awk '{print $1}')
FILE_PATH=$(realpath --no-symlinks -e "$1" 2>/dev/null)
tmux send-keys -t "$pane_id" Escape Escape C-c ":open $FILE_PATH" Enter
tmux switch-client -t "$pane_id"
tmux select-pane -t "$pane_id"
